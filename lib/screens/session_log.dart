import 'package:flutter/material.dart';

class SessionLogPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Session Log', style: TextStyle(fontSize: 24)));
  }
}
