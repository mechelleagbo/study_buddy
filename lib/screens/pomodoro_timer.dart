import 'package:flutter/material.dart';

class PomodoroTimerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('Pomodoro Timer', style: TextStyle(fontSize: 24)));
  }
}
